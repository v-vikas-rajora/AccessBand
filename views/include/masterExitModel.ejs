<style>
    div#exitModel button {
        background-color: #fff;
        border: none;
        color: #4C6A92;
    }


    /* Modal styles */
    div#exitModel .modal-content {
        background-color: #fefefe;
        margin: 2% auto;
        padding: 40px;
        border: 1px solid #888;
        width: 80%;
        position: relative;
        height: 60%;
        overflow-y: auto;
    }

    /* Table area styles */
    div#exitModel .exitTableAreaa {
        overflow: auto;
        height: 70%;
        overflow: auto;
    }

    /* Table styles */
    div#exitModel .exitDataa {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
        overflow: auto;
    }

    div#exitModel .exitDataa th,
    div#exitModel .exitDataa td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
    }

    div#exitModel .exitDataa th {
        background-color: #f2f2f2;
    }

    /* Center align Date and Time columns */
    div#exitModel .exitDataa th:last-child {
        width: 130px;
    }


    /* Close button */
    div#exitModel .close {
        color: #aaa;
        font-size: 28px;
        font-weight: bold;
        position: absolute;
        top: 10px;
        right: 40px;
        transition: 0.3s;
    }

    div#exitModel .close:hover,
    div#exitModel .close:focus {
        color: black;
        text-decoration: none;
        cursor: pointer;
    }

    /* Overlay background for modal */
    div#exitModel {
        display: none;  /* Ensure the modal is hidden initially */
        position: fixed;  /* Position it fixed on the screen */
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.6); /* Light black color (semi-transparent) */
        z-index: 1000; /* Ensure it's on top of other content */
    }

    /* Modal content container */
    div#exitModel .modal-content {
        position: relative;
        background-color: #fefefe;
        margin: 10% auto;
        padding: 40px;
        border: 1px solid #888;
        width: 80%;
        height: 60%;
        overflow-y: auto;
    }

</style>

    <div id="exitModel" class="modal exit_model">
        <div class="modal-content">
            <span class="close" onclick="closeMasterExit()">&times;</span>
    
            <div class="exitTableAreaa">
                <table class="exitDataa" id="aaaaaaa">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Mobile</th>
                            <th>Email</th>
                            <th>Date In</th>
                            <th>Time In</th>
                            <th>Master Card User</th>
                            <th>Post</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
                
            </div>
        </div>
    </div>