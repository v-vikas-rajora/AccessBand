<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AccessBand</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #f1f1f1, #a8c0ff);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .login-container {
            background-color: white;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 340px;
            text-align: center;
        }
        h2 {
            color: #4C6A92;
            font-size: 24px;
            margin-bottom: 20px;
        }
        .input-field {
            width: 100%;
            padding: 12px;
            margin: 15px 0;
            border: 2px solid #ddd;
            border-radius: 6px;
            font-size: 16px;
            box-sizing: border-box; /* Ensures padding and border are included in width */
        }
        .input-field:focus {
            outline: none;  /* Remove default outline */
            border-color: #ddd; /* Keep border color same as normal state */
        }
        /* Removed border color change on focus */
        .login-btn {
            width: 100%;
            padding: 14px;
            background-color: #4C6A92;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 18px;
            margin-top: 10px; /* To maintain consistent space */
            box-sizing: border-box; /* Ensures padding is included in width */
        }
        .login-btn:hover {
            background-color: #3b5575;
        }
        .footer-links {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
            margin-top: 20px;
        }
        .footer-links a {
            color: #4C6A92;
            text-decoration: none;
        }
        .footer-links a:hover {
            text-decoration: underline;
        }
        .alert-warning{
            background-color: #3b5575;
            text-align: left;
            color: #664d03;
            background-color: #fff3cd;
            padding: 15px;
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>AccessBand - Hive Login</h2>
        <button class="login-btn" onclick="loginWithHive()">Login with Hive</button>
        <div class="footer-links"></div>
    </div>
</body>
</html>

<script>
    function loginWithHive() {
        // ✅ Directly Redirect to Hive Signer (without prompt)
        window.location.href = "https://hivesigner.com/oauth2/authorize?client_id=accessband&redirect_uri=https://accessband-production.up.railway.app/login/hive-callback&response_type=token&scope=login";
    }
</script>

